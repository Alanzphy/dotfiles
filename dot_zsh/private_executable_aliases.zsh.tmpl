
# --- COMMON ALIASES ---
# Zsh config
alias zshconfig='nvim ~/.zshrc'
alias ohmyzsh='nvim ~/.oh-my-zsh'
alias zshalias='nvim ~/.zsh/aliases.zsh'
alias v='nvim'

# Git
alias gs='git status --short --branch'
alias gd='git diff'
alias gl="git log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset)%C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all"
alias gcheck='git checkout'
alias gpush='git push'
alias gc='git clone'
alias gpull='git pull'
alias gshove='git push --force-with-lease --force-if-includes'
alias lzg='lazygit'

# Docker & K8s
alias dup='docker compose up'
alias run='docker compose run'
alias ddown='docker compose down'
alias dbuild='docker build'
alias dbuildx='docker buildx'
alias dockerusage='docker system df'
alias lzd='lazydocker'
alias dockerprune='docker system prune -a'
alias k='kubectl'
alias kgp='kubectl get pods -o wide'
alias kgall='kubectl get all'

# Modern Tools (Bat/Eza)
alias bat='bat -p'
alias cat='bat -p'
alias tree='eza --tree'

# Interactive
alias cp='cp -i'
alias mv='mv -i'
alias mkdir='mkdir -v'

# Networking / Utils / System
alias myip='curl http://ipecho.net/plain; echo'
alias usage='du -h -d1'
alias dev='cd ~/Dev/'
alias usage1='du -h -d1 | grep -v "/$" | sort -hr'
alias runport='sudo lsof -i'
alias runport1='sudo lsof -i -P -n | grep'
alias dirs='dirs -v | head -10'
alias hs='history -i | grep -i'
alias history='history -i'
alias runpid='sudo lsof -p'
alias df='df -h'
alias psa="ps auxf"
alias psgrep="ps aux | grep -v grep | grep -i -e VSZ -e"
alias free="free -mt"
alias userlist="cut -d: -f1 /etc/passwd | sort"
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# agent containers
alias opencodebox='opencode-project'
alias opencodebox_isolate='opencode-project --all-isolate'


# EXtractor Function
ex () {
  if [ -f $1 ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1   ;;
      *.tar.gz)    tar xzf $1   ;;
      *.bz2)       bunzip2 $1   ;;
      *.rar)       unrar x $1   ;;
      *.gz)        gunzip $1    ;;
      *.tar)       tar xf $1    ;;
      *.tbz2)      tar xjf $1   ;;
      *.tgz)       tar xzf $1   ;;
      *.zip)       unzip $1     ;;
      *.Z)         uncompress $1;;
      *.7z)        7z x $1      ;;
      *.deb)       ar x $1      ;;
      *.tar.xz)    tar xf $1    ;;
      *.tar.zst)   tar xf $1    ;;
      *)           echo "'$1' cannot be extracted via ex()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}


# --- ARCH LINUX SPECIFIC ---

{{- if eq .chezmoi.os "linux" }}

# Pacman / Yay / Paru
alias pac="pacman --color auto"
alias update='yay -Syyu' # En Arch update es full upgrade
alias big="expac -H M '%m\t%n' | sort -h | nl"
alias rip="expac --timefmt='%Y-%m-%d %T' '%l\t%n %v' | sort | tail -200 | nl"
alias cleanup='sudo pacman -Rns $(pacman -Qtdq)'
alias unlock="sudo rm /var/lib/pacman/db.lck"
alias pathpkg='yay -Ql'
alias riplong="expac --timefmt='%Y-%m-%d %T' '%l\t%n %v' | sort | tail -3000 | nl"


# Logs & Systemd
alias jctl="journalctl -p 3 -xb"
alias sysfailed="systemctl list-units --failed"
alias lpacman="bat /var/log/pacman.log"
alias lxorg="bat /var/log/Xorg.0.log"
alias lxorgo="bat /var/log/Xorg.0.log.old"



# System
alias probe="sudo -E hw-probe -all -upload"
alias update-grub="sudo grub-mkconfig -o /boot/grub/grub.cfg"
alias rmunused='sudo pacman -Rcns $(pacman -Qtdq)'
alias unhblock="hblock -S none -D none"
alias pbcopy='xclip -selection clipboard'
alias pbpaste='xclip -selection clipboard -o'
alias open='xdg-open'
alias openb='xdg-open'
alias hw="hwinfo --short"
alias pacman="pacman --color auto"
alias rm='trash-put'
alias rmdir='trash-put'


# Config edits
alias epacman="sudo $EDITOR /etc/pacman.conf"
alias egrub="sudo $EDITOR /etc/default/grub"
alias emkinitcpio="sudo $EDITOR /etc/mkinitcpio.conf"
alias nfstab="sudo $EDITOR /etc/fstab"
alias nhostname="sudo $EDITOR /etc/hostname"
alias eb="$EDITOR ~/.bashrc"
alias ez="$EDITOR ~/.zshrc"



{{- end }}


# --- ESPECÍFICO DE MACOS ---
{{- if eq .chezmoi.os "darwin" }}

# Brew
alias b="brew"
alias update='brew update; brew upgrade; brew cu -a;  brew cleanup'
alias bup='brew update; brew upgrade'
alias bcask='brew cu -a'
alias bprune='brew cleanup --prune=all'


# System
alias flushdns="sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder"
alias open='open'
alias rm='trash -v'    # Requiere 'brew install trash'
alias rmdir='trash -v'

{{- end }}
